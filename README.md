# Product-Inventory-Service
A lightweight inventory management REST API built with FastAPI and SQLite, supporting CRUD operations, stock management, and transactional inventory logging with thread-safe execution.
